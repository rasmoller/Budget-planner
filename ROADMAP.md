# Roadmap

The roadmap is the source of truth for what to build next. See `docs/development-flow.md` for how features move through the roadmap into the codebase.

## Currently working on

Features being implemented and worked on right now. **Ongoing tasks are the highest priority.**

- **Delete button for income/expense items on the budget page** — Add a delete button next to the edit button for each income and expense item on the budget page (`/`). Items can currently only be deleted from inside the edit dialog.

## Upcoming features

The next features to pick from once the current work is done.

- (empty — add features here as they are proposed)

## Feature groups

Bundles of closely-related features that should be handled together in the future, e.g. a new set of pages that share a workflow and would be built as one effort.

- (empty — add groups here as they are identified)
